---
import BaseLayout from "../layouts/BaseLayout.astro";
import PageHero from "../components/PageHero.astro";
import SectionHeader from "../components/SectionHeader.astro";
import RouteStage from "../components/RouteStage.astro";
import TechIcon from "../components/TechIcon.astro";
import { routeStages, techStack } from "../data/ruta.js";
---

<BaseLayout title="Ruta de aprendizaje | Chilete DevPath">
  <PageHero
    eyebrow="Ruta de aprendizaje"
    title="Empieza por fundamentos y avanza con práctica real"
    description="Aquí ordeno el camino como me hubiera gustado encontrarlo: primero entender, luego practicar y después construir con más criterio."
    variant="ruta"
    visualTitle="Mapa de avance"
    visualText="Ruta visual de aprendizaje."
    imageSrc="/assets/img/heros/ruta.png"
    imageAlt="Cuaderno oscuro con una ruta visual de aprendizaje técnico por etapas."
  />

  <section class="seccion">
    <div class="contenedor">
      <div class="ruta-etapas">
        {routeStages.map((stage) => <RouteStage stage={stage} />)}
      </div>
      <div class="ruta-accion">
        <p>El contenido práctico vive en el repositorio de aprendizaje. Lo seguiré ampliando con ejercicios que tengan sentido, no con carpetas de relleno.</p>
        <a class="boton boton-principal" href="https://github.com/chiletedevpath/aprendizaje" target="_blank" rel="noopener">
          Abrir aprendizaje
        </a>
      </div>
    </div>
  </section>

  <section class="seccion">
    <div class="contenedor">
      <SectionHeader
        eyebrow="Tecnologías en práctica"
        title="Herramientas que aparecen en la ruta"
        description="Estas tecnologías aparecen porque ayudan a resolver algo dentro de la ruta. No están para decorar una lista, sino para practicar con propósito."
      />
      <div class="tech-grid" aria-label="Tecnologias usadas en Chilete DevPath">
        {techStack.map((tech) => (
          <a
            class={`tech-badge tech-${tech.tone}`}
            href={tech.href}
            target="_blank"
            rel="noopener"
            aria-label={`Ver aprendizaje relacionado con ${tech.name}`}
          >
            <span class="tech-icono"><TechIcon name={tech.name} /></span>
            <div>
              <strong>{tech.name}</strong>
              <p>{tech.description}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
