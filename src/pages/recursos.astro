---
import BaseLayout from "../layouts/BaseLayout.astro";
import SectionHeader from "../components/SectionHeader.astro";
import { resources } from "../data/recursos.js";
---

<BaseLayout title="Recursos | Chilete DevPath">
  <section class="seccion page-hero seccion-clara">
    <div class="contenedor">
      <SectionHeader
        eyebrow="Recursos"
        title="Material para aprender y practicar"
        description="Recursos organizados por utilidad: practicar, resolver, consultar y publicar con criterio."
      />
    </div>
  </section>

  <section class="seccion">
    <div class="contenedor">
      <div class="tarjetas tarjetas-4">
        {resources.map((resource) => (
          <article class="tarjeta recurso-card">
            <span class="estado">{resource.status}</span>
            <small>{resource.type}</small>
            <h3>{resource.title}</h3>
            <p>{resource.description}</p>
            <a href={resource.href} target="_blank" rel="noopener">Ver recurso</a>
          </article>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
